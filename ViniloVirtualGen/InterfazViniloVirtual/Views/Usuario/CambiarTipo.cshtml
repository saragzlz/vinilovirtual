@using ViniloVirtualGen.ApplicationCore.Enumerated.ViniloVirtual
@model InterfazViniloVirtual.Models.UsuarioViewModel



@{
    ViewData["Title"] = "Editar Perfil";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center">Cambiar los privilegios del usuario</h1>

<hr />
<div class="row justify-content-center">
    <div class="col-md-4">
        <form asp-action="CambiarTipo">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TipoEnum" class="control-label"></label>
                <br />
                @Html.DropDownListFor(m => m.TipoEnum,
                         new SelectList(Enum.GetValues(typeof(TipoUsuarioEnum))),
                "Selecciona privilegios",  new { @class = "custom-select rounded-pill" })
                <span asp-validation-for="TipoEnum" class="text-danger"></span>
            </div>
            <div class="form-group mt-2">
                <a asp-action="Index"><button type="button" class="btn btn-secondary rounded-pill">Cancelar</button></a>
                <input type="submit" value="Save" class="btn btn-primary rounded-pill" />
            </div>
        </form>
    </div>
</div>



@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
